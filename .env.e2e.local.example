# Copy to .env.e2e.local (gitignored, covered by *.local) and fill in real
# values. Only needed for the admin-CRUD golden path in test/e2e/; the
# public catalog browse/search tests run without any credentials.

E2E_ADMIN_LOGIN=
E2E_ADMIN_PASSWORD=
